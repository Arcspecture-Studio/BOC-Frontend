public static class BinanceConfig
{
    public static readonly int UPDATE_LISTEN_KEY_INTERVAL = 60 * 30;
    public static readonly float TRAILING_MIN_PERCENTAGE = 0.1f;
    public static readonly float TRAILING_MAX_PERCENTAGE = 5f;
}